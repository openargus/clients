[Unit]
Description=Argus SQL updater %I
After=syslog.target network.target

[Service]
Type=simple
Restart=always
RestartSec=5
ExecStartPre=/sbin/rasqlinsert-setup start %i
ExecStart=/usr/bin/xargs -a /run/argus/rasqlinsert.%i /usr/bin/rasqlinsert -S localhost:561 -X
ExecStopPost=/sbin/rasqlinsert-setup stop %i

[Install]
WantedBy=multi-user.target
